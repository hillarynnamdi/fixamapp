<% provide(:title, 'Edit Area | Gofixam')%>
<div class="admin_wrapper">

    <div class="admin-header">
      <%=render 'bookings/navbar' %>
    </div>

  <div class="container">
<h1>Editing area</h1>

<%= render 'form' %>

<%= link_to 'Show', @area %> |
<%= link_to 'Back', city_path(@area.city) %>

</div>
</div>

<%=render "shared/footer_scroll2"%>